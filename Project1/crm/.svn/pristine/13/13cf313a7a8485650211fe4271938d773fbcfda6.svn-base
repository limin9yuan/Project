var prefixbugCategory = "/sales/bugCategory"
$().ready(function() {
	loadDicValue("sales_gategory","category");
	loadCrmDataValue("/project/moduleCategory/listDic","bugCategory");
	loadCrmDataValue("/project/productCategory/listDic","productId");
	loadCrmDataValue("/project/moduleCategory/listDic","moduleId");
	validateRule();
	bugCategoryMapper_edit();
});

$.validator.setDefaults({
	submitHandler : function() {
		update();
	}
});
function update() {
	$.ajax({
		cache : true,
		type : "POST",
		url : "/sales/bugCategory/update",
		data : $('#signupForm').serialize(),// 你的formid
		async : false,
		error : function(request) {
			parent.layer.alert("Connection error");
		},
		success : function(data) {
			if (data.code == 0) {
				parent.layer.msg("操作成功");
				parent.reLoad();
				var index = parent.layer.getFrameIndex(window.name); // 获取窗口索引
				parent.layer.close(index);

			} else {
				parent.layer.alert(data.msg)
			}

		}
	});

}
function validateRule() {
	var icon = "<i class='fa fa-times-circle'></i> ";
	$("#signupForm").validate({
		rules : {
			elseCategory : {
				required : true,
				maxlength:50
			}
		},
		messages : {
			elseCategory : {
				required : icon + "请输入其他分类！",
				maxlength:icon + "字符长度不能大于50"
			}
		}
	})
}
//修改——显示数据绑定
function bugCategoryMapper_edit(){
	$.ajax({
		url : prefixbugCategory + '/edit_ajax/' + $("#bugCategory").val(),
		type : "get",
		data : {
			'bugId' : $("#bugCategory").val(),
		},
		success : function(data) {
			var result = data.bugCategory;
			$("input[name='bugId']").val(result.bugId);
			$("input[name='newCategory']").val(result.newCategory);
			$("input[name='newCategoryContent']").val(result.newCategoryContent);
			$("input[name='elseCategory']").val(result.elseCategory);
			$("textarea[name='requirementDescription']").val(result.requirementDescription);
			$("textarea[name='bugRemarks']").val(result.bugRemarks);
			$("select[name='category']").val(result.category);
			$("select[name='category']").trigger("chosen:updated");
			$("select[name='bugCategory']").val(result.bugCategory);
			$("select[name='bugCategory']").trigger("chosen:updated");
			$("select[name='productId']").val(result.productId);
			$("select[name='productId']").trigger("chosen:updated");
			$("select[name='moduleId']").val(result.moduleId);
			$("select[name='moduleId']").trigger("chosen:updated");
		}
	});
}